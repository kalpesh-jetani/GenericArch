# Generated tool profiles

**GENERATED — do not hand-edit.** Regenerate from the ledger:

```bash
./Scripts/ga-tool-note.sh --sync
```

The lookup surface for every external platform this product has profiled. Grep it before
reaching a platform; the row names the profile to read and the recipe script to call.

- **Source of truth:** [`.claude/tools/LEDGER.tsv`](../../../tools/LEDGER.tsv) — status, failure count,
  verified date. Edit the ledger, never this note.
- **Rules, lifecycle and schema:** [TOOL-PROFILES.md](../../../../docs/TOOL-PROFILES.md).
- **Fires from:** [`SKILL.md`](../SKILL.md) — one always-on description
  for the whole capability; each profile below loads only when it is needed.

A skill's `references/` directory is for exactly this — material loaded when it is needed rather
than on every session. Nothing here is auto-loaded; `SKILL.md` step 0 routes to it, and `grep`
finds it.

---

## Profiles

| Tool | Profile | Recipe script | Wired? | Status | Last verified |
|---|---|---|---|---|---|
<!-- GA:ROWS — generated by Scripts/ga-tool-note.sh --sync; rows between these markers are overwritten -->
| figma | [`figma.md`](../../../tools/figma.md) — Figma — design files, variables and screenshots, reached through the Figma MCP connector | `Scripts/Generated/figma.sh` | connector | active | 2026-09-09 |
<!-- GA:ROWS-END -->

Empty until the first external platform is actually reached. Nothing is listed in advance —
a profile exists only for a platform that was successfully used.
