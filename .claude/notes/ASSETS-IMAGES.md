# Assets — Images

Every image asset the app ships, where it lives, and what uses it. An index — grep a row, then open
the code it points at.

**Read it when** you need to find an asset, its owner, or whether one is still used — before
grepping the tree.

---

## Inventory

Filled by `/sync-app-notes` from the project. Columns follow the active profile; the shape below is
the common one. Blank until a profile is declared.

| Group | Asset name | Owner | Rendering | Theme variant | Used by |
|---|---|---|---|---|---|
| — | — | — | — | — | — |

---

## Gaps

| Finding | Where | Noted |
|---|---|---|
| — | — | — |

`/sync-app-notes` flags an asset nothing references (a candidate for removal — see
`.claude/notes/.evidence/`) and a reference to an asset that does not exist.
