# Assets — Colors

Every color token the app defines, its values per theme, and its role. Names are semantic (what the
color is *for*), never descriptive (what it looks like).

**Read it when** you need the token for a role, or the value behind a token — before hard-coding a
literal.

---

## Inventory

Filled by `/sync-app-notes` from the project. Columns follow the active profile; the shape below is
the common one. Blank until a profile is declared.

| Token | Light | Dark | Role | Owner |
|---|---|---|---|---|
| — | — | — | — | — |

---

## Gaps

| Finding | Where | Noted |
|---|---|---|
| — | — | — |

`/sync-app-notes` flags a literal color used where a token exists, and a token nothing references.
