# Style Guide

The design tokens the app defines — spacing, radius, and the rest — each pointing at where its
detail lives. An index, not the detail: a token row names a code path or a `styles/<name>.md` spec.

**Read it when** you need the token for a measurement or a component spec — before hard-coding a
literal.

---

## This file is an index, not the detail

| Detail lives in | Use when | Example |
|---|---|---|
| **A code path** *(preferred)* | The style is implemented | — |
| **`styles/<name>.md`** | Agreed but not built, or spans several files | — |

---

## Spacing

Filled by `/sync-app-notes` from the project. Blank until a profile is declared.

| Token | Value | Use for | Detail — *code path, or `styles/…`* |
|---|---|---|---|
| — | — | — | — |

---

## Radius

| Token | Value | Use for | Detail — *code path, or `styles/…`* |
|---|---|---|---|
| — | — | — | — |

---

## Gaps

| Finding | Where | Noted |
|---|---|---|
| — | — | — |

`/sync-app-notes` flags a literal used where a token exists, and a token nothing references.
