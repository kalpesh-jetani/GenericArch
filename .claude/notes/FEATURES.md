# Features & Screens

The features this product ships, each with where its code lives and what it owns. An index, never
read in full — grep a row, then open the code it points at.

**Read it when** you need to find a feature's code, its screens, or which routes it owns — before
grepping the tree.

---

## Features

Filled by `/sync-app-notes` from the project. Columns follow the active profile; the shape below is
the common one. Blank until a profile is declared.

| Feature | Path — *what's there* | Screens | Routes | Presentation | Skill |
|---|---|---|---|---|---|
| — | — | — | — | — | — |

---

## Screens

| Screen | View — *what's there* | Model — *what's there* | Route | States implemented | L10n prefix |
|---|---|---|---|---|---|
| — | — | — | — | — | — |

---

## Gaps

| Finding | Where | Noted |
|---|---|---|
| — | — | — |

`/sync-app-notes` flags a screen with no route, a route no screen renders, and a feature whose code
path resolves to nothing.
