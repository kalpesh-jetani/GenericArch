/// Placeholder so `__NAME__` compiles and tests before it has content.
///
/// Delete this file with the first real type. A package that never loses its placeholder is a
/// package nobody needed.
public enum __NAME__Placeholder {
    /// The package name, so a smoke test has something to assert on.
    public static let moduleName = "__NAME__"
}
