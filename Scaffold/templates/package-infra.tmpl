// swift-tools-version: 6.0
import PackageDescription

// Infrastructure package. Depends downward only — Core, and nothing sideways (CLAUDE.md §3).
let package = Package(
    name: "__NAME__",
__PLATFORMS_BLOCK__
    products: [.library(name: "__NAME__", targets: ["__NAME__"])],
    dependencies: [.package(path: "../Core")],
    targets: [
        .target(name: "__NAME__", dependencies: ["Core"], swiftSettings: [.swiftLanguageMode(.v6)]),
        .testTarget(name: "__NAME__Tests", dependencies: ["__NAME__"], swiftSettings: [.swiftLanguageMode(.v6)]),
    ]
)
